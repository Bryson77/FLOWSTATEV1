name: Deploy Mobile to Expo (EAS Update)

on:
  push:
    branches:
      - main
    paths:
      - 'apps/mobile/**'
      - 'lib/**'
      - '.github/workflows/expo-deploy.yml'
  workflow_dispatch:
    inputs:
      branch:
        description: 'EAS channel / branch to publish update to'
        required: true
        default: 'preview'

concurrency:
  group: ${{ github.workflow }}-${{ github.ref }}
  cancel-in-progress: true

jobs:
  update:
    name: EAS Update
    runs-on: ubuntu-latest

    steps:
      - name: ⬇️ Checkout Repository
        uses: actions/checkout@v4

      - name: 🟢 Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: 20

      - name: 📦 Setup pnpm
        uses: pnpm/action-setup@v3
        with:
          version: 9

      - name: 🚀 Setup Expo and EAS CLI
        uses: expo/expo-github-action@v8
        with:
          eas-version: latest
          token: ${{ secrets.EXPO_TOKEN }}

      - name: 💾 Get pnpm store directory
        shell: bash
        run: |
          echo "STORE_PATH=$(pnpm store path --silent)" >> $GITHUB_ENV

      - name: ⚡ Cache pnpm dependencies
        uses: actions/cache@v4
        with:
          path: ${{ env.STORE_PATH }}
          key: ${{ runner.os }}-pnpm-store-${{ hashFiles('**/pnpm-lock.yaml') }}
          restore-keys: |
            ${{ runner.os }}-pnpm-store-

      - name: 📥 Install Dependencies
        run: pnpm install --frozen-lockfile

      - name: 🏗️ Build Shared Packages
        run: |
          pnpm --filter @flowstate/shared build || true
          pnpm --filter @flowstate/study-engine build

      - name: 🔍 Typecheck Mobile App
        working-directory: apps/mobile
        run: pnpm run typecheck

      - name: 🚀 Publish EAS Update
        working-directory: apps/mobile
        run: |
          BRANCH_NAME="${{ github.event.inputs.branch || 'production' }}"
          eas update --auto --branch "$BRANCH_NAME" --message "Deploy from GitHub Actions (commit ${{ github.sha }})"
